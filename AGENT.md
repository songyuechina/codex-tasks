# Codex Tasks 工作區規範

**版本**: v1.6
**更新日期**: 2025-11-18

---

## 一、行為規範
### 快捷命令

- jst：等效於執行指令“按即时对话.txt的内容执行”，即按照 cad/即时对话.txt 的所有流程要求來操作。遇到該命令時，必須立即依照即时对话規範進行。


此文檔定義 `D:/codex-tasks` 工作區的統一要求。

### 第一規範：CAD 即時對話要求

來源：`D:/codex-tasks/cad/即时对话.txt`。所有 CAD 文件操作、函數開發、測試任務必須在啟動後先完成以下步驟，否則視為違規：

1. **基礎導入**：
   ```python
    import CAD_basic as cb
   from CAD_file_operations import *
   ```
2. **七個基礎范式**（每步都需 `cad_zt_zero()` 清場並按流程執行保存/關閉）：
   - 新建 DWG → `draw_line()` → `save_file()` → `close_file()`
   - 打開 DWG → `draw_circle()` → `save_file()` → `close_file()`
   - `copy_file_content_pywin32(B, A)` 完整插入
   - `insert_region_from_file(...)` 依指定窗口與目標點插入
   - 打開文件後 `draw_tarch_wall(...)`
   - 在已有牆上 `insert_tarch_door(...)`
   - 在已有牆上 `insert_tarch_window(...)`
3. **必用選擇/屬性接口**：`select_entities_in_window`、`stc`、`get_object_property`、`set_object_property`。
4. **函數節點必有日誌**；若測試失敗，根據反饋迭代直至成功。  
5. 以上內容需在所有 AGENT.md 中保留，視為項目第一規範。
6. 函數專屬資料夾規範：每個新函數必須在其 Functional_control 子目錄下建立獨立資料夾，內含該函數本體、測試腳本、測試 DWG 副本與測試報告/日志（例如：functions/<fn>/tests、logs、scripts）。每輪測試需記錄日期、DWG 副本名稱、測試坐標、目標厚度、成功/失敗情況，並保存在函數資料夾的 tests/logs 子目錄中。
7. **超時回退（命令級）**：任何單條 CAD 命令（例如一次 `draw_tarch_wall`、`TOpening`、`insert_region_from_file` 等）阻塞超過 120 秒，必須立即執行 `cad_zt_zero()`，回到 0 進程初始狀態後再重試；該限制針對每次命令發送，而非整個函數流程。

---

## 二、進度彙報與狀態檢查

(原內容略)


## 函數標準化強化規範

當需要分析或改進某個 CAD 函數（例如 
ew_file、open_file、ctivate_document_by_name 等）時，必須遵循以下流程：

1. **理解並更新源函數**：前往主腳本（通常是 cad/scripts/CAD_file_operations.py 或對應文件）查找並修改目標函數，確保主程式始終保持最新版本。
2. **建立專屬資料夾結構**：在 cad/Functional_control/Functions_ CAD_basic/<函數名>/ 下建立對應資料夾，至少包含：
   - 函數源码副本（便於查閱/複製）。
   - 可在 IDLE/命令行直接運行的示例腳本與命令（.txt/.py）。
   - test_log.txt 等測試記錄文件，記錄時間、操作指令、結果。
   - **多函數子資料夾要求**：若同一功能資料夾（例如 insert_tests）涵蓋多個函數，必須為每個函數建立獨立子資料夾（名稱應與函數一致），並將該函數的示例腳本與測試 DWG 全部放在該子資料夾中，避免互相覆寫或引用錯誤。
   - **說明文件**：每個函數及其子資料夾必須包含 README.md（或等價 .md 說明），第一行寫完整的命令列示例（如 `python demo.py source.dwg target.dwg`），其餘段落需描述測試目的、輸出檔、清理/還原步驟。
   - **命令列與檔案保護**：函數測試腳本必須透過命令列顯式接收函數需要的參數（例如源/目標 DWG 路徑）；除函數本身的效果外，不得主動覆蓋或清空測試 DWG。如需預設樣例，可提供單獨的樣板檔案，僅在檔案不存在時初始化。
   - **測試檔案還原**：命令列示例必須可重複運行；每次測試後要把示例 DWG 還原為模板狀態（刪除臨時檔並重新從 shared 模板複製），確保下次運行依然得到同樣的初始條件。
   - **CAD 狀態流程（強制）**：所有測試腳本必須嚴格遵循 `cad_zt_oneb()` → `litz()` → 執行目標函數操作 → 關閉與本次函數相關的文件 → 再次 `cad_zt_oneb()` 的流程，以確保開始與結束時 CAD 桌面都回到「天正開啟一個未保存 DWG」的基準狀態。
3. **生成並運行示例**：通過專屬腳本或命令生成示例 DWG（或其他測試資料），運行測試並將輸出、DWG、副檔保存在專屬資料夾。
4. **更新操作說明**：在 CAD文件操作示範.txt 中新增條目，需寫明功能用途、函數名稱、專屬資料夾、示例命令/腳本與調用流程，確保與實際資料夾內容一致。
5. **測試與記錄**：每完成一次測試，必須在對應專屬資料夾內的 	est_log（或等同文件）記錄測試時間、DWG 名稱、步驟和結果；若測試失敗，記錄原因並重新執行直至成功。
6. **持續沿用**：下次對話或新增函數時，必須沿用上述流程，保持文件結構、文檔與示例一致，保證任何人都能依照資料夾中的腳本重現同樣的流程。

此規範與「七個基礎範式」同級，後續所有函數處理任務均須遵照執行。

## 函數測試規範

- 參考文件：`D:/codex-tasks/函数测试规范.txt`。所有 CAD 函式任務在開始動手前必須研讀該文件，並逐條遵守。
- 規範要求依序完成：函數與參數分析 → 測試輸入/DWG 自動構建（命令列腳本、模板回滾）→ `jst` + `CAD文件操作示範.txt` 流程運行 → 在專屬資料夾 README 與 `tests/test_log.txt` 記錄最新成功案例 → 實際運行 README 首段的每一條命令（含 Fire 調用/`cd /d`/`cmd /c` 前綴），確認輸出正確後才可寫入。
- 不得以「無樣例」「未現場測試」等語句結束任務；若測試失敗，必須修復並重跑直至取得成功記錄。
- 如需新增條款，先更新 `函数测试规范.txt`，再在此 AGENT 檔同步說明，確保後續對話沿用最新規則。
