python align_text_to_horizontal_line_demo.py --y-position 200 --align-side 下边界

脚本新建 DWG 并写入两段文字，调用 align_text_to_horizontal_line() 进行水平对齐，流程遵循 cad_zt_oneb → litz → 操作 → cad_zt_oneb，日志写入 test_log.txt。
