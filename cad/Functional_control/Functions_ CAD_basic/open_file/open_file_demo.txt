from pathlib import Path
import sys

SCRIPT_PATH = Path(__file__).resolve()
CAD_DIR = SCRIPT_PATH.parents[4]
SYSTEM_DIR = CAD_DIR / "system"
SCRIPTS_DIR = CAD_DIR / "scripts"
for extra in (SYSTEM_DIR, SCRIPTS_DIR):
    extra_str = str(extra)
    if extra_str not in sys.path:
        sys.path.insert(0, extra_str)

import CAD_file_operations as cfo

def run_demo():
    target = CAD_DIR / "Functional_control" / "Functions_ CAD_basic" / "new_file_draw_shapes" / "shapes_demo_213859.dwg"
    print(f">>> 调用 open_file('{target}')")
    ok = cfo.open_file(str(target))
    print(f"结果: {ok}")

if __name__ == "__main__":
    run_demo()
