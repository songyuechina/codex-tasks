python close_file_no_save_demo.py close_file_no_save_demo.dwg

流程：cad_zt_oneb() → litz() → 从 shared 复制 close_demo_b.dwg → open_file() → save_file() → close_file('no_save') → cad_zt_oneb()。
日志写入 test_log.txt。
