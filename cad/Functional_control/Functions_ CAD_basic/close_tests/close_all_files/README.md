python close_all_files_demo.py close_all_demo_a.dwg close_all_demo_b.dwg

命令执行：cad_zt_oneb() → litz() → 复制 shared 模板到两个文件 → open_file() 打开两次 → close_all_files('auto_save') → 重复打开 → close_all_files('no_save') → cad_zt_oneb() 复位。
记录写入 test_log.txt。
