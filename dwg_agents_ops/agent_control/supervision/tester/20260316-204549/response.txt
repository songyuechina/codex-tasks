1. 测试范围  
- 基于 `print_verifier.py` 已给出的代码片段，当前证据最多能支持：  
  - 对**已存在的输出 PDF**，按 `output_path` 关联到打印 job；  
  - 从 job 的 `media` 字段中解析期望纸张尺寸；  
  - 读取 **PDF 第 1 页** 的 `mediabox` 尺寸并换算为 mm；  
  - 在容差内做“期望纸张尺寸 vs PDF 页尺寸”匹配。  
- 因此，在“真实打印 49/49 成功 + PDF 页尺寸 49/49 匹配”前提下，**目前只能支持到：这 49 个案例的输出 PDF 首页面幅规格与 job 里声明的纸张规格一致（限可解析样本）**。  
- **不能据此扩展**为：图框内容正确、比例正确、方向正确、居中正确、裁剪正确、线宽正确、全部页都正确、复杂工程整体打印结果完全正确。

2. 测试用例 / 执行  
- 已有证据可视为一次**半自动校验**：  
  - Setup：49 个真实打印成功案例，对应 PDF 输出存在。  
  - Steps：用 `print_verifier.py` 对 `media` 与 PDF 页尺寸做比对。  
  - Expected：49/49 匹配。  
- 最小补充测试建议：  
  1) **自动/半自动**：确认这 49 份 PDF 是否都是单页；若有多页，需补查**每一页**尺寸，而不是只看第一页。  
     - 预期：所有页尺寸都与预期一致。  
  2) **自动**：列出 `media` 解析失败 / PDF 读取失败数量（如果有）。  
     - 预期：失败数为 0，否则 49/49 结论只对“成功解析子集”成立。  
  3) **人工抽样**：从 49 份里抽 2~3 个复杂案例，目视确认图框方向/是否被裁切。  
     - 预期：页面尺寸正确且内容未明显旋转、缩放异常或裁切。

3. 结果 / 阻塞  
- 结论：当前证据**可以支持“这批案例输出 PDF 的纸张规格正确”这一较窄结论**，但更准确地说，应限定为：  
  - **PDF 页面物理尺寸正确**；  
  - 且按现有代码片段，**更像是第一页尺寸正确**。  
- 最小缺口：  
  - 是否校验了**多页 PDF 的所有页**；  
  - 是否存在**`media`/PDF 未解析样本**；  
  - 是否需要最小人工确认**内容未裁切/方向未错**。  
- 阻塞：当前只给了 `print_verifier.py` 的**截断片段**，无法确认后续逻辑是否还有补充统计、容差处理或异常过滤，因此结论范围必须保守。

[PROGRESS] task=none; status=blocked; completion=85; next=补充确认49份PDF是否全为单页且无解析失败样本，即可把结论稳定收敛到“该批案例PDF纸张规格正确”。
